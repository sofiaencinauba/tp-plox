Para correr las benchmarks de Ruby, ejecutar desde la raiz del proyecto:
```bash
docker compose run --rm -T ruby ruby benchmarks/benchmark_ruby.rb
```

Para correr las benchmarks de Python, ejecutar desde la raiz del proyecto:
```bash
python3 benchmarks/benchmark_python.py
```